# qr-code-reader-and-writer
QR code Reader and Writer

## Pre-requisities
in comand line
* py -m venv .venv

## Installation dependencies
* pip install numpy 
* pip install qrcode[pil]
* pip install matplotlib
